# مصادر البيانات والنشر في Okami-BOT

## وضع التشغيل الحالي

يعمل المشروع الآن في وضع **البيانات الحقيقية المتحققة**. جرى إلغاء الاعتماد على النتائج والصور والفصول التجريبية، كما أزيلت المصادر التي كانت تخفي أخطاء الحماية أو تغيّر بنية المواقع خلف بيانات مولدة. المصدر المتاح هو **AniList** لجلب بيانات وصفية حقيقية للمانغا؛ مثل العنوان والغلاف والوصف والحالة والعدد الإجمالي للفصول إن توفّر.

> لا يقدّم AniList صور صفحات الفصول أو روابط نشر مرخصة لها؛ لذلك يمنع Okami بدء طابور نشر للصور من هذا المصدر. هذه حماية مقصودة كي لا يظهر محتوى غير متحقق منه أو يعاد نشر محتوى لا تملك حق نشره.

| الوظيفة | الحالة | الملاحظات |
|---|---:|---|
| البحث عن أعمال مانغا | متاح | يجلب نتائج حقيقية من AniList. |
| العنوان والوصف والغلاف والحالة | متاح | يعرض من المصدر مع رابط العمل الأصلي. |
| عدد الفصول المسجل | متاح عند توفره | هو عدد وصفي، وليس قائمة صفحات قابلة للنشر. |
| صور الفصول أو إعادة نشرها | غير متاح | لا يوجد مصدر مرخص ومتكامل لذلك في المشروع. |
| نتائج أو صور مولدة عند فشل المصدر | معطلة | يفشل الطلب برسالة واضحة بدل اختلاق محتوى. |

## النشر في فيسبوك

يستخدم المشروع **Pages API** الرسمية للنشر على صفحة فيسبوك فقط. توثيق Meta يعرّف إنشاء المنشورات على أنه طلب إلى المسار `/{page_id}/feed`، ويشترط توكن صفحة وصلاحية `pages_manage_posts`، إضافة إلى أن يكون صاحب التوكن قادرًا على تنفيذ مهمة `CREATE_CONTENT` في الصفحة. [1] [2]

لا يعتمد المشروع على كوكيز المتصفح، ولا يخزنها، ولا يدعم النشر الآلي في الملف الشخصي. يُمنع هذا التصميم من إدخال بيانات جلسة حساسة إلى التطبيق ويجعل إعدادات النشر قابلة للمراجعة والتدوير من خلال متغيرات البيئة.

| متغير البيئة | الغرض |
|---|---|
| `FB_PAGE_ACCESS_TOKEN` | توكن صفحة رسمي للنشر والمراسلة من الصفحة. |
| `FB_PAGE_ID` | معرّف الصفحة التي سيظهر المنشور باسمها. |
| `FB_VERIFY_TOKEN` | قيمة التحقق من Webhook؛ أنشئ قيمة طويلة وعشوائية. |
| `FB_GRAPH_API_VERSION` | إصدار Graph API؛ القيمة الافتراضية الحالية هي `v26.0`. |

## الإعداد والاختبار

انسخ `.env.example` إلى `.env` وأضف القيم في بيئة التشغيل فقط، من دون رفع ملف `.env` إلى Git. بعد منح تطبيق Meta الصلاحيات اللازمة وربطه بالصفحة، شغّل اختبارًا غير منشور للتحقق من المصدر وإعدادات النشر:

```bash
pnpm install --frozen-lockfile --ignore-scripts
node scripts/verify-verified-sources.js "Solo Leveling"
```

يعرض الاختبار نتيجة بحث حقيقية وتفاصيل عمل حقيقية، ويتأكد من أن جلب صور الفصول مرفوض صراحةً. كما يفحص قابلية توكن الصفحة للتحقق، لكنه **لا ينشئ منشورًا**.

## إضافة مصدر جديد

لا تضف مصدرًا جديدًا إلا إذا كان لديك تصريح واضح باستعمال واجهته أو بياناته للنشر. يجب أن يعيد المصدر بيانات حقيقية فقط، وأن يبلّغ عن أخطاء الوصول أو تغيّر البنية بدل تحويلها إلى نتائج وهمية. إذا كان المصدر يوفر فقط بيانات وصفية، فيجب تعيين `publishingAllowed: false` كي يبقى طابور النشر متوقفًا.

## المراجع

[1]: https://developers.facebook.com/documentation/pages-api/posts "Meta for Developers — Posts"
[2]: https://developers.facebook.com/docs/graph-api/reference/page/feed/ "Meta for Developers — Page Feed"
[3]: https://docs.anilist.co/guide/graphql/queries/media "AniList API Docs — Media queries"
